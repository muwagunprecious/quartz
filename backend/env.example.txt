# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/campusmart?schema=public"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-this-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Redis Configuration (for Socket.IO scaling and BullMQ)
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""

# File Upload Configuration
UPLOAD_MODE="local" # Options: "local" or "s3"
UPLOAD_MAX_FILE_SIZE="5242880" # 5MB in bytes
UPLOAD_MAX_FILES="10"
UPLOAD_ALLOWED_TYPES="image/jpeg,image/png,image/webp,image/jpg"

# Local Storage (when UPLOAD_MODE=local)
UPLOAD_LOCAL_PATH="./uploads"
UPLOAD_LOCAL_URL="http://localhost:5000/uploads"

# AWS S3 Configuration (when UPLOAD_MODE=s3)
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="your-aws-access-key"
AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
AWS_S3_BUCKET="campusmart-uploads"
AWS_S3_URL="https://campusmart-uploads.s3.amazonaws.com"

# Encryption (for NIN and sensitive data)
ENCRYPTION_KEY="your-32-character-encryption-key-here-change-this"
ENCRYPTION_IV="your-16-char-iv"

# Application Configuration
NODE_ENV="development" # Options: "development", "production", "test"
PORT="5000"
CORS_ORIGIN="http://localhost:3000" # Frontend URL

# Rate Limiting
THROTTLE_TTL="60" # Time window in seconds
THROTTLE_LIMIT="10" # Max requests per TTL

# Notification Configuration
NOTIFICATION_RETENTION_DAYS="30" # How long to keep notifications

# Admin Configuration
ADMIN_EMAIL="admin@campusmart.com"
ADMIN_PASSWORD="admin123" # Change this in production!

# Optional: Email Configuration (for password reset, notifications)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM=""
